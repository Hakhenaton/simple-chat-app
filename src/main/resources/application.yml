spring:
  devtools:
    restart:
      exclude: []
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  liquibase:
    change-log: classpath:/database/changelog.xml